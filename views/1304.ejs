<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        LeetCode 1304: Find N Unique Integers Sum up to Zero (C++)
    </title>
    <link rel="stylesheet" href="/styles.css" />
    <script src="/solution.js"></script>
</head>
<body>
    <main>
        <h1>LeetCode 1304: Find N Unique Integers Sum up to Zero (C++)</h1>
        <div class="problem-description">
            <p><strong>Problem:</strong>
                Given an integer n, return any array containing n unique integers such that they add up to 0. The array must contain exactly n integers, all integers must be unique (no duplicates), and the sum of all integers must equal 0.
            </p>
            <p><strong>Example:</strong>
                Input: n = 5 → Output: [1, -1, 2, -2, 0]. This array has 5 unique integers and their sum is 1 + (-1) + 2 + (-2) + 0 = 0. Other valid answers include [-2, -1, 0, 1, 2] or [-5, -1, 1, 2, 3].
            </p>
        </div>
        <div class="code-container">
            <button class="copy-button" onclick="copyCode(this)">Copy Code</button>
            <pre><code class="language-cpp">class Solution {
public:
    vector&lt;int&gt; sumZero(int n) {
        vector&lt;int&gt; result;
        result.reserve(n); // Pre-allocate space for efficiency
        
        // Strategy: Create pairs of positive and negative numbers
        // Each pair (i, -i) sums to 0, maintaining overall sum of 0
        
        // Generate n/2 pairs of opposite integers
        int pairsCount = n / 2;
        for (int i = 1; i &lt;= pairsCount; i++) {
            result.push_back(i);    // Add positive number
            result.push_back(-i);   // Add corresponding negative number
        }
        
        // Handle odd case: if n is odd, add 0 to complete the array
        // Adding 0 doesn't change the sum but gives us exactly n elements
        if (n % 2 == 1) {
            result.push_back(0);
        }
        
        return result;
    }
};</code></pre>
        </div>
        <a href="/" class="back-link">← Back to Home</a>
    </main>
</body>
</html>