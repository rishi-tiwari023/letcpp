<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        LeetCode 1390: Four Divisors (C++)
    </title>
    <link rel="stylesheet" href="/styles.css" />
    <script src="/solution.js"></script>
</head>

<body>
    <main>
        <h1>LeetCode 1390: Four Divisors (C++)</h1>
        <div class="problem-description">
            <p><strong>Problem:</strong>
                Given an integer array nums, return the sum of divisors of the integers in that array that have exactly
                four
                divisors. If there is no such integer in the array, return 0. A number has exactly four divisors when it
                is
                either the cube of a prime (p^3 has divisors: 1, p, p^2, p^3) or the product of two distinct primes (p*q
                has
                divisors: 1, p, q, p*q). The solution efficiently finds all divisors by iterating only up to the square
                root
                and counting both the divisor and its pair.
            </p>
            <p><strong>Example:</strong>
                Input: nums = [21,4,7] → Output: 32. 21 has 4 divisors (1,3,7,21) with sum 32. 4 has 3 divisors (1,2,4).
                7 has 2 divisors (1,7). Total = 32.
                Input: nums = [21,21] → Output: 64. Both 21s contribute 32 each.
                Input: nums = [1,2,3,4,5] → Output: 0. None have exactly 4 divisors.
            </p>
        </div>
        <div class="code-container">
            <button class="copy-button" onclick="copyCode(this)">Copy Code</button>
            <pre><code class="language-cpp">class Solution {
public:
    int calcSum(int val) {
        int divCnt = 0, divSum = 0;
        for (int d = 1; d * d &lt;= val; d++) {
            if (val % d == 0) {
                divCnt++;
                divSum += d;
                // Add the paired divisor if it's different
                if (d != val / d) {
                    divCnt++;
                    divSum += val / d;
                }
            }
        }
        // Only valid if number has exactly 4 divisors
        return (divCnt == 4) ? divSum : 0;
    }
    int sumFourDivisors(vector&lt;int&gt;&amp; nums) {
        int ans = 0;
        for (int x : nums) {
            ans += calcSum(x);
        }
        return ans;
    }
};</code></pre>
        </div>
        <a href="/" class="back-link">← Back to Home</a>
    </main>
</body>

</html>