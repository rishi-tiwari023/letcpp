<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        LeetCode 3637: Trionic Array I
    </title>
    <link rel="stylesheet" href="/styles.css" />
    <script src="/solution.js"></script>
</head>

<body>
    <main>
        <h1>LeetCode 3637: Trionic Array I (C++)</h1>
        <div class="problem-description">
            <p><strong>Problem:</strong>
                An array is called trionic if it can be divided into three non-empty parts such that the first part is
                strictly increasing, the second part is strictly decreasing, and the third part is strictly increasing.
                Return true if the given array is trionic, otherwise return false.
            </p>
            <p><strong>Example:</strong>
                Input: arr = [1,3,2,4]<br>
                Output: true<br>
                Explanation: The array can be divided as [1] (increasing), [3,2] (decreasing), [4] (increasing), making
                it trionic.
            </p>
        </div>
        <div class="code-container">
            <button class="copy-button" onclick="copyCode(this)">Copy Code</button>
            <pre><code class="language-cpp">class Solution {
public:
    bool isTrionic(vector&lt;int&gt;&amp; arr) {
        int n = arr.size();
        if (n &lt;= 3) return false;
        for (int i = 1; i &lt; n - 2; i++) {
            for (int j = i + 1; j &lt; n - 1; j++) {
                bool inc1 = true, dec = true, inc2 = true;
                // first increasing
                for (int k = 0; k &lt; i; k++)
                    if (arr[k] &gt;= arr[k + 1]) inc1 = false;
                // decreasing
                for (int k = i; k &lt; j; k++)
                    if (arr[k] &lt;= arr[k + 1]) dec = false;
                // second increasing
                for (int k = j; k &lt; n - 1; k++)
                    if (arr[k] &gt;= arr[k + 1]) inc2 = false;
                if (inc1 &amp;&amp; dec &amp;&amp; inc2) return true;
            }
        }
        return false;
    }
};</code></pre>
        </div>
        <a href="/" class="back-link">← Back to Home</a>
    </main>
</body>

</html>