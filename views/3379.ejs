<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        LeetCode 3379: Transformed Array
    </title>
    <link rel="stylesheet" href="/styles.css" />
    <script src="/solution.js"></script>
</head>

<body>
    <main>
        <h1>LeetCode 3379: Transformed Array (C++)</h1>
        <div class="problem-description">
            <p><strong>Problem:</strong>
                You are given an integer array nums. Create a transformed array result of the same length where
                result[i] is obtained by moving nums[i] steps from index i. If nums[i] is positive, move right (wrapping
                around to the beginning if needed). If nums[i] is negative, move left (wrapping around to the end if
                needed). If nums[i] is 0, result[i] = nums[i].
            </p>
            <p><strong>Example:</strong>
                Input: nums = [3,-2,1,1]<br>
                Output: [1,1,1,3]<br>
                Explanation: For i=0, move 3 steps right: 0→1→2→3, result[0]=nums[3]=1. For i=1, move 2 steps left:
                1→0→3, result[1]=nums[3]=1. For i=2, move 1 step right: 2→3, result[2]=nums[3]=1. For i=3, move 1 step
                right: 3→0, result[3]=nums[0]=3.
            </p>
        </div>
        <div class="code-container">
            <button class="copy-button" onclick="copyCode(this)">Copy Code</button>
            <pre><code class="language-cpp">class Solution {
public:
    vector&lt;int&gt; constructTransformedArray(vector&lt;int&gt;&amp; nums) {
        int n = nums.size();
        vector&lt;int&gt; result(n);
        for (int i = 0; i &lt; n; i++) {
            int steps = nums[i];
            int curr = i;
            // Move one step at a time
            if (steps &gt; 0) {
                while (steps--) {
                    curr = (curr + 1) % n;
                }
            } else {
                while (steps++) {
                    curr = (curr - 1 + n) % n;
                }
            }
            result[i] = nums[curr];
        }
        return result;
    }
};</code></pre>
        </div>
        <a href="/" class="back-link">← Back to Home</a>
    </main>
</body>

</html>