<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        LeetCode 3634: Minimum Removals to Balance Array
    </title>
    <link rel="stylesheet" href="/styles.css" />
    <script src="/solution.js"></script>
</head>

<body>
    <main>
        <h1>LeetCode 3634: Minimum Removals to Balance Array (C++)</h1>
        <div class="problem-description">
            <p><strong>Problem:</strong>
                You are given an integer array nums and an integer k. An array is considered balanced if for every pair
                of elements nums[i] and nums[j] where i &lt; j, the condition nums[i] * k &gt;= nums[j] holds. Return
                the minimum number of elements you need to remove to make the array balanced.
            </p>
            <p><strong>Example:</strong>
                Input: nums = [2,1,6,4], k = 3<br>
                Output: 1<br>
                Explanation: After sorting: [1,2,4,6]. Remove 6 to get [1,2,4] where 1*3≥2 (false), so we need different
                approach. Remove 1 to get [2,4,6] where 2*3≥4 (true) and 4*3≥6 (true). Minimum removals = 1.
            </p>
        </div>
        <div class="code-container">
            <button class="copy-button" onclick="copyCode(this)">Copy Code</button>
            <pre><code class="language-cpp">class Solution {
public:
    int minRemoval(vector&lt;int&gt;&amp; nums, int k) {
        int size = nums.size();
        vector&lt;int&gt; removals(size, 0);
        // Sort to make range comparisons easier
        sort(nums.begin(), nums.end());
        for (int right = 1; right &lt; size; right++) {
            // Find the first index where nums[left] * k &gt;= nums[right]
            int left = lower_bound(
                nums.begin(),
                nums.end() - (size - 1 - right),
                ceil((double)nums[right] / k)
            ) - nums.begin();
            // Either remove current element or remove all elements before `left`
            removals[right] = min(removals[right - 1] + 1, left);
        }
        return removals[size - 1];
    }
};</code></pre>
        </div>
        <a href="/" class="back-link">← Back to Home</a>
    </main>
</body>

</html>