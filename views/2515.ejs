<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        LeetCode 2515: Shortest Distance to Target String in a Circular Array (C++)
    </title>
    <link rel="stylesheet" href="/styles.css" />
    <script src="/solution.js"></script>
</head>

<body>
    <main>
        <h1>LeetCode 2515: Shortest Distance to Target String in a Circular Array (C++)</h1>
        <div class="problem-description">
            <p><strong>Problem:</strong>
                You are given a 0-indexed circular string array <code>words</code> and a string <code>target</code>.
                A circular array means that the end of the array connects to the beginning of the array.
                Formally, the next element of <code>words[i]</code> is <code>words[(i + 1) % n]</code> and the
                previous element of <code>words[i]</code> is <code>words[(i - 1 + n) % n]</code>, where <code>n</code>
                is the length of <code>words</code>.
                Starting from <code>startIndex</code>, you can move to either the next word or the previous word
                with 1 step at a time. Return the shortest distance needed to reach the string <code>target</code>.
                If the string <code>target</code> does not exist in <code>words</code>, return -1.
            </p>
            <p><strong>Example:</strong>
                Input: words = ["hello","i","am","leetcode","hello"], target = "hello", startIndex = 1 → Output: 1.
                We can reach "hello" by moving 1 step left (to index 0) or 3 steps right (to index 4).
                The shortest distance is 1.
            </p>
        </div>
        <div class="code-container">
            <button class="copy-button" onclick="copyCode(this)">Copy Code</button>
            <pre><code class="language-cpp">class Solution {
public:
    int closestTarget(vector&lt;string&gt;&amp; arr, string key, int start) {
        int size = arr.size();
        int best = INT_MAX;

        for (int idx = 0; idx &lt; size; idx++) {
            if (arr[idx] == key) {
                // direct distance vs wrap-around distance
                int direct = abs(idx - start);
                int wrap   = size - direct;

                best = min(best, min(direct, wrap));
            }
        }

        return best == INT_MAX ? -1 : best;
    }
};</code></pre>
        </div>
        <a href="/" class="back-link">← Back to Home</a>
    </main>
</body>

</html>