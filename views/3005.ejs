<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        LeetCode 3005: Count Elements With Maximum Frequency (C++)
    </title>
    <link rel="stylesheet" href="/styles.css" />
    <script src="/solution.js"></script>
</head>

<body>
    <main>
        <h1>LeetCode 3005: Count Elements With Maximum Frequency (C++)</h1>
        <div class="problem-description">
            <p><strong>Problem:</strong>
                You are given an array nums consisting of positive integers. Return the total frequencies of elements in
                nums such that those elements all have the maximum frequency. The frequency of an element is the number
                of occurrences of that element in the array.
            </p>
            <p><strong>Example:</strong>
                Input: nums = [1,2,2,3,1,4] → Output: 4. Elements 1 and 2 each have frequency 2, which is the maximum
                frequency. So total count is 2 + 2 = 4.
            </p>
        </div>
        <div class="code-container">
            <button class="copy-button" onclick="copyCode(this)">Copy Code</button>
            <pre><code class="language-cpp">class Solution {
public:
    int maxFrequencyElements(vector&lt;int&gt;&amp; nums) {
        unordered_map&lt;int, int&gt; frequency;
        
        for (int num : nums) {
            frequency[num]++;
        }
        
        int maxFreq = 0;
        for (const auto&amp; pair : frequency) {
            maxFreq = max(maxFreq, pair.second);
        }
        
        int result = 0;
        for (const auto&amp; pair : frequency) {
            if (pair.second == maxFreq) {
                result += pair.second;
            }
        }
        
        return result;
    }
};</code></pre>
        </div>
        <a href="/" class="back-link">← Back to Home</a>
    </main>
</body>

</html>