<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        LeetCode 2435: Paths in Matrix Whose Sum Is Divisible by K (C++)
    </title>
    <link rel="stylesheet" href="/styles.css" />
    <script src="/solution.js"></script>
</head>

<body>
    <main>
        <h1>LeetCode 2435: Paths in Matrix Whose Sum Is Divisible by K (C++)</h1>
        <div class="problem-description">
            <p><strong>Problem:</strong>
                You are given a 0-indexed m x n integer matrix grid and an integer k. You are currently at position (0,
                0)
                and you want to reach position (m - 1, n - 1) moving only right or down at each step. A path in the
                matrix
                is good if the sum of the elements on the path is divisible by k. Return the number of good paths in the
                matrix. Since the answer may be too large, return it modulo 10^9 + 7.
            </p>
            <p><strong>Example:</strong>
                Input: grid = [[5,2,4],[3,0,5],[0,7,2]], k = 3 → Output: 2. There are two paths where the sum of
                elements
                is divisible by 3.
                Input: grid = [[0,0]], k = 5 → Output: 1. There is one path (the only path) and its sum is 0, divisible
                by 5.
                Input: grid = [[7,3,4,9],[2,3,6,2],[2,3,7,0]], k = 1 → Output: 10. When k=1, all paths are valid since
                any
                sum is divisible by 1.
            </p>
        </div>
        <div class="code-container">
            <button class="copy-button" onclick="copyCode(this)">Copy Code</button>
            <pre><code class="language-cpp">class Solution {
public:
    int rec(int i,int j,int r,vector&lt;vector&lt;int&gt;&gt;&amp; g,int&amp; k,
            vector&lt;vector&lt;vector&lt;int&gt;&gt;&gt;&amp; dp){
        int n=g.size(), m=g[0].size();
        if(i==n-1 &amp;&amp; j==m-1) return r%k==0;
        if(dp[i][j][r] != -1) return dp[i][j][r];
        int a=0, b=0;
        if(j+1&lt;m) a = rec(i, j+1, (r + g[i][j+1]) % k, g, k, dp);
        if(i+1&lt;n) b = rec(i+1, j, (r + g[i+1][j]) % k, g, k, dp);
        return dp[i][j][r] = (a + b) % 1000000007;
    }
    int numberOfPaths(vector&lt;vector&lt;int&gt;&gt;&amp; g, int k) {
        int n=g.size(), m=g[0].size();
        if(k==1){
            vector&lt;vector&lt;int&gt;&gt; w(n, vector&lt;int&gt;(m,1));
            for(int i=1;i&lt;n;i++)
                for(int j=1;j&lt;m;j++)
                    w[i][j] = (w[i-1][j] + w[i][j-1]) % 1000000007;
            return w[n-1][m-1];
        }
        vector&lt;vector&lt;vector&lt;int&gt;&gt;&gt; dp(n, vector&lt;vector&lt;int&gt;&gt;(m, vector&lt;int&gt;(k, -1)));
        return rec(0,0,g[0][0] % k, g, k, dp);
    }
};</code></pre>
        </div>
        <a href="/" class="back-link">← Back to Home</a>
    </main>
</body>

</html>