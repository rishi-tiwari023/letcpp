<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        LeetCode 2943: Maximize Area of Square Hole in Grid
    </title>
    <link rel="stylesheet" href="/styles.css" />
    <script src="/solution.js"></script>
</head>

<body>
    <main>
        <h1>LeetCode 2943: Maximize Area of Square Hole in Grid (C++)</h1>
        <div class="problem-description">
            <p><strong>Problem:</strong>
                You are given a grid with n + 2 horizontal bars and m + 2 vertical bars. You can remove some bars to
                create a square hole. The hole is defined by removing consecutive horizontal bars and consecutive
                vertical bars. Return the maximum area of a square hole you can make.
            </p>
            <p><strong>Example:</strong>
                Input: n = 2, m = 1, hBars = [2, 3], vBars = [2]<br>
                Output: 4<br>
                Explanation: We can remove horizontal bars at positions 2 and 3, and vertical bar at position 2 to
                create a 2x2 square hole with area 4.
            </p>
        </div>
        <div class="code-container">
            <button class="copy-button" onclick="copyCode(this)">Copy Code</button>
            <pre><code class="language-cpp">class Solution {
public:
    int calc(vector&lt;int&gt;&amp; b) {
        sort(b.begin(), b.end());
        int mx = 0, cnt = 0;
        for (int i = 0; i &lt; b.size(); i++) {
            if (i == 0 || b[i] != b[i - 1] + 1) cnt = 1;
            else cnt++;
            mx = max(mx, cnt); // update max block
        }
        return mx + 1;
    }
    int maximizeSquareHoleArea(int n, int m, vector&lt;int&gt;&amp; h, vector&lt;int&gt;&amp; v) {
        int a = calc(h);
        int b = calc(v);
        int s = min(a, b); // smallest side decides square
        return s * s;
    }
};</code></pre>
        </div>
        <a href="/" class="back-link">← Back to Home</a>
    </main>
</body>

</html>