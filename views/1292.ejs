<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        LeetCode 1292: Maximum Side Length of a Square with Sum Less than or Equal to Threshold
    </title>
    <link rel="stylesheet" href="/styles.css" />
    <script src="/solution.js"></script>
</head>

<body>
    <main>
        <h1>LeetCode 1292: Maximum Side Length of a Square with Sum Less than or Equal to Threshold (C++)</h1>
        <div class="problem-description">
            <p><strong>Problem:</strong>
                Given a m x n matrix mat and an integer threshold, return the maximum side-length of a square with a sum
                less than or equal to threshold. If there is no such square, return 0.
            </p>
            <p><strong>Example:</strong>
                Input: mat = [[1,1,3,2,4,3,2],[1,1,3,2,4,3,2],[1,1,3,2,4,3,2]], threshold = 4<br>
                Output: 2<br>
                Explanation: The maximum side length of a square with sum less than or equal to 4 is 2. One such square
                is the top-left 2x2 square with sum 1+1+1+1 = 4.
            </p>
        </div>
        <div class="code-container">
            <button class="copy-button" onclick="copyCode(this)">Copy Code</button>
            <pre><code class="language-cpp">class Solution {
public:
    int maxSideLength(vector&lt;vector&lt;int&gt;&gt;&amp; mat, int threshold) {
        int h = mat.size(), w = mat[0].size();
        vector&lt;vector&lt;long long&gt;&gt; ps(h + 1, vector&lt;long long&gt;(w + 1, 0));
        // Prefix sum computation
        for (int i = 1; i &lt;= h; i++) {
            for (int j = 1; j &lt;= w; j++) {
                ps[i][j] = mat[i - 1][j - 1] + ps[i - 1][j]
                         + ps[i][j - 1] - ps[i - 1][j - 1];
            }
        }
        auto possible = [&amp;](int side) {
            for (int i = side; i &lt;= h; i++) {
                for (int j = side; j &lt;= w; j++) {
                    long long sum =
                        ps[i][j] - ps[i - side][j]
                      - ps[i][j - side] + ps[i - side][j - side];
                    if (sum &lt;= threshold)
                        return true;
                }
            }
            return false;
        };
        int low = 0, high = min(h, w);
        // Binary search for the maximum valid square
        while (low &lt; high) {
            int mid = (low + high + 1) / 2;
            if (possible(mid))
                low = mid;
            else
                high = mid - 1;
        }
        return low;
    }
};</code></pre>
        </div>
        <a href="/" class="back-link">← Back to Home</a>
    </main>
</body>

</html>