<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        LeetCode 2654: Minimum Number of Operations to Make All Array Elements Equal to 1 (C++)
    </title>
    <link rel="stylesheet" href="/styles.css" />
    <script src="/solution.js"></script>
</head>

<body>
    <main>
        <h1>LeetCode 2654: Minimum Number of Operations to Make All Array Elements Equal to 1 (C++)</h1>
        <div class="problem-description">
            <p><strong>Problem:</strong>
                You are given a 0-indexed array nums consisting of positive integers. You can perform the following
                operation
                on the array any number of times: Select an index i such that 0 <= i < n - 1 and replace either nums[i]
                    or nums[i+1] with their GCD. Return the minimum number of operations required to make all elements
                    of nums equal to 1. If it is impossible, return -1. </p>
                    <p><strong>Example:</strong>
                        Input: nums = [2,6,3,4] → Output: 4. We can perform 4 operations: Replace nums[2] with gcd(3,4)
                        = 1, then
                        replace other elements to get all 1's.
                        Input: nums = [2,10,6,14] → Output: -1. The GCD of any subarray will always be even, so we can
                        never get 1.
                        Input: nums = [8,4,2] → Output: -1. All elements are powers of 2, making it impossible to obtain
                        1.
                    </p>
        </div>
        <div class="code-container">
            <button class="copy-button" onclick="copyCode(this)">Copy Code</button>
            <pre><code class="language-cpp">class Solution {
public:
    int minOperations(vector&lt;int&gt;&amp; nums) {
        int n = nums.size();
        int countOnes = ranges::count(nums, 1);
        // If array already has 1's, we just need to make the rest equal to 1
        if (countOnes &gt; 0)
            return n - countOnes;
        int minStepsToGetOne = INT_MAX;
        // Try every subarray and find the shortest one whose GCD becomes 1
        for (int start = 0; start &lt; n; ++start) {
            int currentGCD = nums[start];
            for (int end = start + 1; end &lt; n; ++end) {
                currentGCD = __gcd(currentGCD, nums[end]);
                if (currentGCD == 1) {
                    minStepsToGetOne = min(minStepsToGetOne, end - start);
                    break; // No need to expand further; already found gcd 1
                }
            }
        }
        // If no subarray gives gcd 1, it's impossible
        // Otherwise, convert the rest to 1 (each needs 1 operation)
        return minStepsToGetOne == INT_MAX ? -1 : minStepsToGetOne + n - 1;
    }
};</code></pre>
        </div>
        <a href="/" class="back-link">← Back to Home</a>
    </main>
</body>

</html>