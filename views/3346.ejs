<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        LeetCode 3346: Maximum Frequency of an Element After Performing Operations I (C++)
    </title>
    <link rel="stylesheet" href="/styles.css" />
    <script src="/solution.js"></script>
</head>

<body>
    <main>
        <h1>LeetCode 3346: Maximum Frequency of an Element After Performing Operations I (C++)</h1>
        <div class="problem-description">
            <p><strong>Problem:</strong>
                You are given an integer array nums and two integers k and numOperations. You must perform an operation
                numOperations times on nums, where in each operation you: Select an index i that was not selected in any
                previous operations, and add an integer in the range [-k, k] to nums[i]. Return the maximum possible
                frequency of any element in nums after performing the operations.
            </p>
            <p><strong>Example:</strong>
                Input: nums = [1,4,5], k = 1, numOperations = 2 → Output: 2. We can achieve a maximum frequency of two
                by adding 0 to nums[1] and -1 to nums[2], making nums = [1,4,4]. Input: nums = [5,11,20,20], k = 5,
                numOperations = 1 → Output: 2. We can achieve a maximum frequency of two by adding 0 to nums[1].
            </p>
        </div>
        <div class="code-container">
            <button class="copy-button" onclick="copyCode(this)">Copy Code</button>
            <pre><code class="language-cpp">class Solution {
public:
    int maxFrequency(vector&lt;int&gt;&amp; nums, int k, int ops) {
        int res = 1;                // Tracks maximum possible frequency
        int curAdj = 0;             // Tracks total adjustable elements in current range
        unordered_map&lt;int, int&gt; freq;  // Stores frequency of each number
        map&lt;int, int&gt; sweep;           // Difference map to simulate range updates
        set&lt;int&gt; vals;                 // Stores all unique candidate positions
        
        // Build frequency map and range difference array
        for (int x : nums) {
            ++freq[x];
            ++sweep[x - k];        // Range start: increase adjustable count
            --sweep[x + k + 1];    // Range end: decrease adjustable count
            vals.insert(x);
            vals.insert(x - k);
            vals.insert(x + k + 1);
        }
        
        // Simulate sweeping over all candidate points
        for (int x : vals) {
            curAdj += sweep.contains(x) ? sweep[x] : 0;    // Update current adjustable count
            int selfFreq = freq.contains(x) ? freq[x] : 0; // Existing frequency of 'x'
            int adjOnly = curAdj - selfFreq;               // Elements that can be turned into 'x'
            res = max(res, selfFreq + min(ops, adjOnly));  // Best possible frequency at this point
        }
        
        return res;
    }
};</code></pre>
        </div>
        <a href="/" class="back-link">← Back to Home</a>
    </main>
</body>

</html>